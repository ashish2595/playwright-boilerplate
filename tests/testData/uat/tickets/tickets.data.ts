import { credentials } from "../credentials";

export const searchTicket = {
    user: credentials.user1,
    organization: "demo-organzation",
    ticketName: "File download links expired prematurely",
    ticketStatus: "In Progress"
}